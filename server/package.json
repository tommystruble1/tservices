{
  "name": "tservices-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Better Auth + Stripe backend for tservices.cc. Deployed separately from the static site — see server/README.md.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-auth": "^1.2.0",
    "zod": "^3.24.0",
    "pg": "^8.12.0",
    "stripe": "^17.0.0"
  }
}
